from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Webscrapping data from sites and using some api to generate data too.',
    author='luisgzaparolli',
)
